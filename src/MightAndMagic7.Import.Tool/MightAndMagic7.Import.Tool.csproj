<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
    <EnforceCodeStyleInBuild>true</EnforceCodeStyleInBuild>
    <AnalysisLevel>latest</AnalysisLevel>
    <Deterministic>true</Deterministic>
    <AllowUnsafeBlocks>false</AllowUnsafeBlocks>
    <IsPackable>false</IsPackable>
    <AssemblyName>mm7import</AssemblyName>
    <RootNamespace>MightAndMagic7.Import.Tool</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="../MightAndMagic7.Import/MightAndMagic7.Import.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- The import suite proves that the packs this tool writes are packs the product loads. -->
    <InternalsVisibleTo Include="MightAndMagic7.Import.Tests" />
  </ItemGroup>
</Project>
